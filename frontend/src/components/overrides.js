import styled from 'styled-components';
import { Container, Row } from '@bootstrap-styled/v4';

export const SpinContainer = styled(Container)`
  padding-bottom: 5rem;
`;

export const RowAlignCenter = styled(Row)`
  align-items: center;
`;

export default {};
